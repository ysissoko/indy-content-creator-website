// Dedicated layout for the CMS admin. Keystatic renders its own full UI here,
// independent of the marketing site's sections.
export default function KeystaticLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
